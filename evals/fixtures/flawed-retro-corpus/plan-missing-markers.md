# Toy flawed plan — retro eval fixture (flaw: missing Phase-5 structure markers)

No execution contract, no Toolchain section, no execution-mode declaration,
no implementer tags, no Uses: lines, no Shared resources section, no Open
Questions section, and no review gate — every element
`scripts/plan-structure-lint.sh` checks for is absent on purpose.

## Tasks
- Task 1 — add a rate limiter to the API. Do it well.
- Task 2 — write a test for it.
