# Toy flawed plan — retro eval fixture (flaw: stale ledger)

This task cites ledger entry E7, but `ledger.md` in this same corpus stops
at E2 — the task was added after the last ledger persist and the ledger was
never re-persisted before it was cited.

## Tasks
- Task 1 — per ledger entry E7, rotate the API credentials.
